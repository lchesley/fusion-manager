@model IEnumerable<FusionManager.Models.CompendiumEntry>

@{
    ViewBag.Title = "Compendium";
}

<h2>Compendium</h2>

<p>
    @Html.ActionLink("Add Persona to Compendium", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.PersonaName)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.ActualLevel)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.InheritedSkills)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.PersonaName)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.ActualLevel)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.InheritedSkills)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { /* id=item.PrimaryKey */ }) |
            @Html.ActionLink("Details", "Details", new { /* id=item.PrimaryKey */ }) |
            @Html.ActionLink("Delete", "Delete", new { /* id=item.PrimaryKey */ })
        </td>
    </tr>
}

</table>
